# Lexicon Sync Plan: v0.10.0-beta.4 → v0.10.0-beta.11

This document outlines the step-by-step plan for syncing the SDK with the latest lexicon changes. Each change will be
implemented, validated, and reviewed before proceeding to the next.

## Current Status

- **Old version**: 0.10.0-beta.4
- **New version**: 0.10.0-beta.11
- **Dependency updated**: ✅ Yes
- **Build status**: ✅ Passing
- **Test status**: ✅ All tests passing (617/617)

## Changes to Implement

### Change 1: Collection Item Weights (beta.7)

**CHANGELOG Reference**: PR #113 - Refactor collection items structure to support optional weights

**What Changed**:

- Collection `items` array changed from array of strongRefs to array of item objects
- Each item now has `itemIdentifier` (strongRef) and optional `itemWeight` (string)
- Activity weights moved from activity schema to collection items

**SDK Tasks**:

- [x] Add usage examples in method documentation showing weighted items (already has example in createCollection)
- [x] Verify `CollectionItemInput` helper type is correct (already supports itemWeight)
- [x] Verify CRUD operations support itemWeight (createCollection/updateCollection already pass items through)
- [x] Add/update tests for collections with weighted items
- [x] Build and test
- [x] Create changeset (minor - new feature available)

**Validation**:

- [x] Format check passes (`pnpm format:check`)
- [x] Lint passes (`pnpm lint`)
- [x] Typecheck passes (`pnpm typecheck`)
- [x] Build passes (`pnpm build`)
- [x] Tests pass (`pnpm test`)
- [x] Types export correctly

**Status**: ✅ Complete

---

### Change 2: Rich Text Facets for Activity Descriptions (beta.7)

**CHANGELOG Reference**: PR #91 - Add rich text facet support to activity claim descriptions

**What Changed**:

- Added `shortDescriptionFacets` and `descriptionFacets` fields to activity lexicon
- Supports mentions, URLs, hashtags, etc. in activity descriptions

**SDK Tasks**:

- [x] Import `AppBskyRichtextFacet` from `@atproto/api` in interfaces.ts
- [x] Add `shortDescriptionFacets` and `descriptionFacets` fields to `CreateHypercertParams`
- [x] Update `create()` method in `HypercertOperationsImpl` to handle facet fields
- [ ] Add JSDoc documentation to `create()` method about facets
- [ ] Add usage examples in method documentation showing how to create facets for mentions, links, and tags
- [x] Add tests for creating hypercerts with facets
- [x] Verify facets are properly validated by lexicon validator
- [x] Build and test
- [x] Create changeset (minor - new feature available)

**Note**: Method documentation for facets is still needed but tests are complete.

**Validation**:

- [x] Format check passes (`pnpm format:check`)
- [x] Lint passes (`pnpm lint`)
- [x] Typecheck passes (`pnpm typecheck`)
- [x] Build passes (`pnpm build`)
- [x] Tests pass (`pnpm test` - 102 tests in HypercertOperationsImpl)
- [x] Types export correctly

**Status**: ✅ Complete

---

### Change 3: Collection Avatar and Banner (beta.7)

**CHANGELOG Reference**: PR #106 - Add avatar and banner fields to collection lexicon

**What Changed**:

- Collections now have `avatar` and `banner` fields for visual representation
- These are image fields (Uri, SmallImage, or LargeImage)

**SDK Tasks**:

- [x] Verify `CreateCollectionParams` already supports avatar/banner (already supports)
- [x] Verify `createCollection()` and `updateCollection()` methods handle avatar/banner (already implemented)
- [ ] Add JSDoc documentation to `createCollection()` and `updateCollection()` methods about avatar
- [ ] Add usage examples in method documentation showing avatar in collections and projects
- [x] Add/update tests for collections with avatar and banner
- [x] Build and test
- [x] Create changeset (minor - new feature available)

**Note**: Method documentation mentions banner but not avatar. Tests are complete.

**Validation**:

- [x] Format check passes (`pnpm format:check`)
- [x] Lint passes (`pnpm lint`)
- [x] Typecheck passes (`pnpm typecheck`)
- [x] Build passes (`pnpm build`)
- [x] Tests pass (`pnpm test` - 632 tests in sdk-core, 136 in sdk-react)
- [x] Types export correctly

**Status**: ✅ Complete

---

### Change 4: Work Scope Logic Expression System (beta.8)

**CHANGELOG Reference**: PR #107 - Add work scope logic expression system with boolean operators

**What Changed**:

- New work scope logic AST with boolean operators (workScopeAll, workScopeAny, workScopeNot, workScopeAtom)
- Work scope tag lexicon for reusable scope atoms
- Activity `workScope` field now uses union type for logic expressions

**SDK Tasks**:

- [ ] Add type exports for work scope expression types:
  - `HypercertWorkScopeAll` = `OrgHypercertsDefs.WorkScopeAll`
  - `HypercertWorkScopeAny` = `OrgHypercertsDefs.WorkScopeAny`
  - `HypercertWorkScopeNot` = `OrgHypercertsDefs.WorkScopeNot`
  - `HypercertWorkScopeAtom` = `OrgHypercertsDefs.WorkScopeAtom`
  - `HypercertWorkScopeExpression` (union of above four)
- [ ] Add type exports for work scope tag:
  - `CreateWorkScopeTagParams`
  - `UpdateWorkScopeTagParams`
  - `WorkScopeTagParams`
- [ ] Verify `CreateHypercertParams.workScope` already supports the union type
- [ ] Add comprehensive documentation about work scope expressions
- [ ] Add usage examples showing how to build AND/OR/NOT expressions
- [ ] Add examples showing how to create and reference work scope tags
- [ ] Add/update tests for work scope expressions and work scope tags
- [ ] Build and test
- [ ] Create changeset (minor - new feature available)

**Validation**:

- [ ] Format check passes (`pnpm format:check`)
- [ ] Lint passes (`pnpm lint`)
- [ ] Typecheck passes (`pnpm typecheck`)
- [ ] Build passes (`pnpm build`)
- [ ] Tests pass (`pnpm test`)
- [ ] Types export correctly
- [ ] Work scope types are usable

**Status**: ⏳ Pending

---

### Change 5: Collection Location Property (beta.11)

**CHANGELOG Reference**: PR #123 - Add location property to collections

**What Changed**:

- Collections now have a direct `location` property (strongRef)
- Replaces the sidecar pattern which was impractical
- Location records can now be referenced directly in collections

**SDK Tasks**:

- [x] Import `AppBskyRichtextFacet` from `@atproto/api` in interfaces.ts
- [x] Add `shortDescriptionFacets` and `descriptionFacets` fields to `CreateHypercertParams`
- [x] Update `create()` method in `HypercertOperationsImpl` to handle facet fields
- [x] Add comprehensive documentation to `HypercertClaim` type about facets
- [x] Add usage examples showing how to create facets for mentions, links, and tags
- [x] Verify facets are properly validated by lexicon validator
- [x] Build and test
- [x] Create changeset (minor - new feature available)

**Validation**:

- [x] Format check passes (`pnpm format:check`)
- [x] Lint passes (`pnpm lint`)
- [x] Typecheck passes (`pnpm typecheck`)
- [x] Build passes (`pnpm build`)
- [x] Tests pass (`pnpm test`)
- [x] Types export correctly

**Status**: ✅ Complete

---

### Change 6: Final Changeset and Documentation

**Tasks**:

- [ ] Review all individual changesets created in steps 1-5
- [ ] Consider whether they should be combined into a single changeset or kept separate
- [ ] Update any top-level documentation if needed
- [ ] Final build and test of entire SDK
- [ ] Prepare summary of all changes for user review

**Validation**:

- [ ] Format check passes (`pnpm format:check`)
- [ ] Lint passes (`pnpm lint`)
- [ ] Typecheck passes (`pnpm typecheck`)
- [ ] All builds pass (`pnpm build` - sdk-core and sdk-react)
- [ ] All tests pass (`pnpm test`)
- [ ] Changesets are properly formatted
- [ ] Documentation is complete

**Status**: ⏳ Pending

---

## Notes

- All export names are already correct (underscore_case)
- Work scope tag constants are already correct (no HELPER\_ prefix)
- Project/collection/location changes are already handled in SDK code
- Each change will be implemented, validated, and reviewed before proceeding to the next
